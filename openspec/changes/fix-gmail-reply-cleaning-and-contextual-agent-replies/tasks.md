## Tests

- [x] Add tests for Gmail quoted-history cleanup.
- [x] Add tests for contextual follow-up replies that do not reuse the initial template.
- [x] Add tests for reply-specific safety validation.
- [x] Add tests that supplier reply prompts include `docs/ooo.md`.
- [x] Add tests that unusable first model output is corrected by a second AI call, not a static fallback.
- [x] Add tests that initial supplier outreach is generated by the configured model provider, not a hardcoded template.
- [x] Add tests that unusable initial/follow-up model outputs fail instead of using static fallback text.
- [x] Add tests that follow-up replies asking for requisites reject repeated initial outreach content.
- [x] Add tests that failed Gmail auto-reply attempts do not remain queued.

## Backend

- [x] Strip quoted Gmail history before storing inbound messages.
- [x] Add follow-up reply safety validation separate from initial outreach validation.
- [x] Replace initial-template reuse in reply mode with contextual employee-like model output.
- [x] Ensure worker uses reply-specific policy for conversation replies.
- [x] Auto-send contextual AI replies for matched inbound Gmail messages regardless of prior approval mode.
- [x] Use `docs/ooo.md` as authoritative company knowledge for supplier follow-up prompts.
- [x] Replace static contextual reply handling with a second model-generated corrected reply.
- [x] Start backend-owned periodic Gmail inbound sync when Gmail inbound is configured.
- [x] Prevent concurrent Gmail sync calls from sending duplicate auto replies for the same inbound message.
- [x] Remove hardcoded supplier outreach templates from `agent.py`.
- [x] Remove hardcoded contextual follow-up fallback replies from `agent.py`.
- [x] Route first supplier contact generation through the local `ModelProvider`.
- [x] Point Docker backend/worker to the host Ollama runtime that has the configured local model installed.
- [x] Strengthen follow-up validation so supplier questions are answered instead of re-asking the initial sourcing checklist.
- [x] Mark Gmail auto-reply attempts running/failed/sent deterministically.

## Frontend

- [x] Make product-details Gmail sync request automatic AI replies by default and stop persisting approval-required mode.
- [x] Remove the static supplier-message preview from product details.

## Verification

- [x] Run focused connector/agent/worker tests.
- [x] Run full Python test suite.
- [x] Validate OpenSpec change.
- [x] Restart local project and smoke-check API/WebUI.
- [x] Re-run focused model-only supplier message tests.
- [x] Re-run full Python test suite.
- [x] Validate updated OpenSpec changes.
- [x] Rebuild/restart local project and smoke-check API/WebUI.
